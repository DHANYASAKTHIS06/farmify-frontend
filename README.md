
  # Website generation

  This is a code bundle for Website generation. The original project is available at https://www.figma.com/design/F7LTwfm5Cm0rL4s6uZx9Ec/Website-generation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  